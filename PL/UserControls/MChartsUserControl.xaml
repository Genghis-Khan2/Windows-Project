<UserControl x:Class="PL.MChartsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:PL"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1000">
    <Grid>
        <StackPanel HorizontalAlignment="Left" MinWidth="550" >
            <lvc:PieChart x:Name="Chart"  Series="{Binding SeriesCollection}"
                      LegendLocation="Right"  InnerRadius="50" HorizontalAlignment="Stretch" VerticalAlignment="Center" MinHeight="450" >
                <lvc:PieChart.ChartLegend>
                    <lvc:DefaultLegend BulletSize="20" HorizontalAlignment="Left"/>
                </lvc:PieChart.ChartLegend>
                <lvc:PieChart.DataTooltip>
                    <lvc:DefaultTooltip BulletSize="20"/>
                </lvc:PieChart.DataTooltip>
            </lvc:PieChart>
        </StackPanel>
        <ComboBox x:Name="DetailcomboBox" HorizontalContentAlignment="Center" IsEditable="True" Text="Choose field" FontSize="25"  Margin="675,125,125,0" VerticalAlignment="Top" Width="200" Height="50" SelectionChanged="DetailcomboBox_SelectionChanged"/>
    </Grid>
</UserControl>
